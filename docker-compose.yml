version: '3.8'

services:
  # PostgreSQL Database
  db:
    image: postgres:16-alpine
    container_name: barvidva-db
    environment:
      POSTGRES_DB: barvidva
      POSTGRES_USER: barvidva
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/migrations:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U barvidva"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  