# Test Environment Configuration
# IMPORTANT: Copy this file to .env.test for running tests
# This keeps test data isolated from development/production data

# Test Database - Use a SEPARATE database for tests!
# Option 1: Separate test database (recommended)
DATABASE_URL=postgres://barvidva:test_password@localhost:5432/barvidva_test?sslmode=disable

# Option 2: Use Docker test container
# DATABASE_URL=postgres://test:test@localhost:5433/barvidva_test?sslmode=disable

# Server Configuration
PORT=8081
ENV=test

# Logging - reduce noise during tests
LOG_LEVEL=error

# Test Authentication Passwords
# Use different passwords than development/production
STAFF_PASSWORD=test_staff_password
ADMIN_PASSWORD=test_admin_password

# Note: Unit tests use mocks and don't require a real database
# Integration tests require this database to be running
